let x = 10;

let isOdd = x % 2 !== 0; // false

// operators
let y = 5 + 3;
let name = "Levi" + "Zwannah"; // LeviZwannah
let j = "James" + 5; // ?? 

let age = 23 - 5;
age = 10 - y;

let product = age * 10;

let q = product / 4;
let m = product / 0;

let rem = 13 % 2; // rem = 1;
rem = 27 % 4; // rem = 3;

let a = 2 ** 3; // a = 8

// x = x + 5;
// x = x - 20;

x += 5; // x = x + 5;
x += 1; // x = x + 1;
x /= 4; // x = x / 4;
x %= 10; // x = x % 10;

x++; // x = x + 1; x += 1;
x--; // x = x - 1; x -= 1;

++x; // x = x + 1;
let r = sum(n) + 3;

let pI = (x) => {
    let prev = x;
    x = x + 1;
    return prev;
}

let p = 1 + pI(x);


let v = 10 + (++x);
v = 10 + (x++);

